# Get your API key from https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxx

# Google OAuth (create credentials at https://console.cloud.google.com/)
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/callback

# Meta / Facebook OAuth for Instagram publishing (create app at https://developers.facebook.com/)
# Instagram account must be Business or Creator and connected to a Facebook Page.
# See README → Instagram Setup for full steps.
META_APP_ID=your-app-id
META_APP_SECRET=your-app-secret
# Optional: app base URL; used to derive callback URL if META_REDIRECT_URI is not set.
# FRONTEND_URL=http://localhost:3000
# FRONTEND_URL=https://your-app.vercel.app
# Optional: set explicitly if you don't use FRONTEND_URL (or Vercel's VERCEL_URL in prod).
META_REDIRECT_URI=http://localhost:3000/api/auth/facebook/callback

# Vercel Blob — for public image URLs used by Instagram API (create Blob store in Vercel dashboard)
# BLOB_READ_WRITE_TOKEN is auto-set when you add the Blob store to the project.
# BLOB_READ_WRITE_TOKEN=
